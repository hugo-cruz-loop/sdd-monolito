# SOLID Review Template

## SOLID Review

| Principle | Status | Evidence | Required Fix |
|---|---|---|---|
| SRP | pass/fail/warning | `{file}:{line}` or design note | Required change or `none` |
| OCP | pass/fail/warning | `{file}:{line}` or design note | Required change or `none` |
| LSP | pass/fail/warning | `{file}:{line}` or design note | Required change or `none` |
| ISP | pass/fail/warning | `{file}:{line}` or design note | Required change or `none` |
| DIP | pass/fail/warning | `{file}:{line}` or design note | Required change or `none` |

**Gate result:** `pass | pass-with-warnings | fail`

## Required Refactor Plan

- {Only include when gate is `fail` or `pass-with-warnings`.}

## Tradeoffs Accepted

- {Document intentional exceptions with reason and risk.}
